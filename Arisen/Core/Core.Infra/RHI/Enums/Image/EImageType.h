#pragma once

namespace ArisenEngine::RHI
{
    typedef enum EImageType
    {
        IMAGE_TYPE_1D = 0,
        IMAGE_TYPE_2D = 1,
        IMAGE_TYPE_3D = 2,
        IMAGE_TYPE_MAX_ENUM = 0x7FFFFFFF
    } EImageType;
}
